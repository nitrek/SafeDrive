<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".SettingsActivity"
    tools:showIn="@layout/activity_setting"
    android:backgroundTint="@color/colorPrimaryDark"
    android:background="@color/colorPrimaryDark"
    >

    <TextView
        android:id="@+id/smsText"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:background="#ccc"
        android:gravity="center"
        android:text="Sms Text to Send"
        android:hapticFeedbackEnabled="true"
        android:textStyle="bold"
        android:imeActionLabel="SMS"
        android:clickable="true"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        android:onClick="smsText"
        />
    <TextView
        android:id="@+id/autoStart"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:background="#ccc"
        android:gravity="center"
        android:layout_below="@+id/smsText"
        android:text="Auto Start Time"
        android:hapticFeedbackEnabled="true"
        android:textStyle="bold"
        android:imeActionLabel="SMS"
        android:clickable="true"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:onClick="autoStart"
        />
    <Switch
        android:id="@+id/notify"
        android:layout_below="@+id/autoStart"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:background="#ccc"
        android:gravity="center"
        android:text="Notification"
        android:hapticFeedbackEnabled="true"
        android:textStyle="bold"
        android:imeActionLabel="SMS"
        android:clickable="true"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        android:onClick="TurnOffNotification"
        />
</RelativeLayout>
